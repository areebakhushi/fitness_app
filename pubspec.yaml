name: workout_planner
description: Elite fitness architecture for the modern athlete.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  firebase_core: ^3.10.1
  firebase_auth: ^5.4.1
  cloud_firestore: ^5.6.1
  google_generative_ai: ^0.4.7
  intl: ^0.19.0
  fl_chart: ^0.70.0
  lucide_icons: ^0.257.0
  font_awesome_flutter: ^11.0.0
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true